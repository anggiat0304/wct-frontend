import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Import komponen utama aplikasi

ReactDOM.render(
  <React.StrictMode>
    <App /> {/* Render komponen utama di sini */}
  </React.StrictMode>,
  document.getElementById('root')
);